{
  "_comment": "ROU-18 — Beispiel-Routing-Tabelle. Kopiere als routing.json (per .gitignore aus dem Repo ausgeschlossen) und passe pro Instanz an. Zwei Abschnitte: `entries` für descriptor-basiertes Matching (ROU-9), `panels` für den App-Panel-Adapter (ROU-24, Konvention-Routing — eine Zeile pro Panel-Instanz, Singular `display_id` nach E-PANEL-5).",
  "entries": [
    {
      "source_id":  "phone:test-1",
      "descriptor": { "figure_id": "rotes-a", "bucket": 0 },
      "display_ids": ["default"],
      "payload":    { "url": "/display/plan/woche?ansicht=klein" }
    },
    {
      "source_id":  "phone:test-1",
      "descriptor": { "figure_id": "rotes-a", "bucket": 1 },
      "display_ids": ["default"],
      "payload":    { "url": "/display/plan/woche" }
    },
    {
      "source_id":  "phone:test-1",
      "descriptor": { "figure_id": "rotes-a", "bucket": 2 },
      "display_ids": ["default"],
      "payload":    { "url": "/display/plan/woche" }
    }
  ],
  "panels": {
    "app-panel:kueche": { "display_id": "tablet-wohnzimmer-01" }
  }
}
