[Service]
# T1389 AUTH-2.a: paired_at-Store-Pfad (OD3). /auth/pair schreibt den paired_at-
# Stempel additiv in denselben geraete.json-Store, den frueher xbuddy-geraete
# fuehrte.
#
# HINWEIS (#1802, gemeldet nicht gefixt): seiten/seiten.service:48 traegt
# dieselbe Zuweisung bereits inline — derselbe Wert an zwei Orten, wie bei
# plan/20-eltern-token.conf. Bereinigung braucht ein eigenes Ticket, weil sie
# die Basis-Vorlage anfasst.
Environment=GERAETE_REGISTRY=__XBUDDY_DATA__/geraete/geraete.json
