# systemd Drop-In — Auth-Bot-Token fuer HART-Auth (T1640).
# Gleiche Begruendung wie bei der finn-Instanz: HART-gegatete Datenrouten
# brauchen ELTERNCHAT_BOT_TOKEN zum Session-Verify.
[Service]
EnvironmentFile=__XBUDDY_DATA__/eltern-chat/.env
