# Bot-Token fuer HART-Auth (T1638). Die familie-Datenrouten sind seit #1638
# AUTH-3-HART (make_require_init_data) — der Cookie/tma-Verify braucht
# ELTERNCHAT_BOT_TOKEN, sonst 500 statt 401/200. familie hatte ihn nie
# (Routen waren vorher unauth). Quelle wie hoerspiel/routine: eltern-chat/.env.
[Service]
EnvironmentFile=__XBUDDY_DATA__/eltern-chat/.env
