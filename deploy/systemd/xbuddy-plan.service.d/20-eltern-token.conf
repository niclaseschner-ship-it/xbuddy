# Bot-Token-Sharing (RAT-16): plan liest die eltern-chat-.env read-only.
#
# HINWEIS (#1802, gemeldet nicht gefixt): plan/plan.service traegt dieselbe
# EnvironmentFile-Zeile bereits inline — derselbe Wert an zwei Orten. Die
# Bereinigung gehoert in ein eigenes Ticket, weil sie die Basis-Vorlage anfasst
# (deploy/systemd/README.md, „Bekannte Vorlagen-Drift").
[Service]
EnvironmentFile=__XBUDDY_DATA__/eltern-chat/.env
