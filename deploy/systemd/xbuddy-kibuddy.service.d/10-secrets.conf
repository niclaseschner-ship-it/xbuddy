# Drop-In fuer xbuddy-kibuddy.service — Geheimnisse (KIBUDDY-21, CONFIG-3).
# Wird durch tools/sync_kibuddy_env.py aus zugangsdaten.json erzeugt.
# Override: das EnvironmentFile aus der Vorlage wird hier ersetzt.
[Service]
EnvironmentFile=__XBUDDY_DATA__/zugangsdaten/kibuddy-env
