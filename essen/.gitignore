# ESSEN-7/ESSEN-21 — Per-Instanz-Dateien des Essens-Buddys, je Instanz separat
# gepflegt, niemals im Repo. Formate sind in den committeten *.example.json
# dokumentiert.
#
# Daten-State-Dateien (ESSEN-7/ESSEN-21) — Per-Instanz, Wunsch-Liste,
# Gerichte-Katalog und Lebensmittel-Override landen ohne ENV-Override neben
# dem Code; alle drei ausgeschlossen.
wuensche.json
gerichte.json
katalog.json

# Per-Instanz-Daten (ESSEN-7) — leben pro Familie, nicht im Repo.
# Default-Pfade wenn essen-Buddy ohne --wuensche-file/--einkaufsliste-file/--zaehler-file
# läuft (Pi-Setup-Folge-Befund 2026-06-12).
einkaufsliste.json
zaehler.json
wuensche.json

# Runtime-Konfig (Bind, Log, Provider, Modell) — Per-Instanz, gitignored
# wie hoerspiel (#1749: Leak-Fläche über alle Services vereinheitlicht).
config.json
